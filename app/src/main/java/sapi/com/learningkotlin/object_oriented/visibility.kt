package sapi.com.learningkotlin.object_oriented


/**
 * 1) Private - Same as in Java
 * 2) Protected - Same as in Java
 * 3) Internal - Visible inside the same module (like here, we only have one module i.e LearningKotlin with cup icon). Usually we only have one module in project.
 * Then in that case it will work like a public.
 * 4) Public  - Same as in Java (Default)
 */
fun main() {

    


}