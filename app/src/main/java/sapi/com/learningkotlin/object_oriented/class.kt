package sapi.com.learningkotlin.object_oriented


/**
 * Class in kotlin is same as in Java. But in Kotlin, in some cases, we really don't need class body. So it means in Kotlin
 * curly braces and class body is optional.
 * To create an object we really don't need new keyword.
 */
fun main() {

    // creating an object of empty class
    val empty = Empty()

}


// we can create a class in Kotlin without having class body
class Empty

// we can have class structure like this in Kotlin
class Filled {
    // TODO add class body
}
